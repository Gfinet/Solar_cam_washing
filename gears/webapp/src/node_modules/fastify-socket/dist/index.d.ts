import { FastifyPluginAsync } from 'fastify';
import { ServerOptions } from 'socket.io';

type FastifySocketioOptions = Partial<ServerOptions> & {
    preClose?: (done: Function) => void;
};
declare const fastifySocketIO: FastifyPluginAsync<FastifySocketioOptions>;

export { type FastifySocketioOptions, fastifySocketIO as default };
